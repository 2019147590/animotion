# 리팩토링 전략

## Problem 1-Pager

### Context

현재 앱은 빌드 없이 `index.html`을 여는 정적 MVP다. `.txt` 조건 파일은 작은 변경 반복, 전체 파일/참조 경로 사전 확인, 300 LOC 이하 파일, 50 LOC 이하 함수, 테스트 추가를 요구한다.

### Problem

`app.js`는 1249 LOC로 필수 기준을 크게 넘고, `styles.css`도 341 LOC로 기준을 넘는다. 기능은 동작하지만 캔버스 렌더링, 선택 편집, 모델 갱신, 파일 I/O, 이벤트 바인딩이 한 파일에 섞여 있어 이후 브러시 마스크나 AI 보조 분리를 추가하기 어렵다.

### Goal

현재 기능과 정적 실행 방식을 유지하면서 JS/CSS를 300 LOC 이하 파일로 분리한다. 순수 geometry 로직에는 Node에서 실행 가능한 테스트를 추가한다.

### Non-Goals

이번 단계에서는 UI 재설계, 신규 애니메이션 기능, AI 세그멘테이션, 브러시 마스크, ES module 전환, 패키지 매니저 도입을 하지 않는다.

### Constraints

- 로컬 `file://` 실행을 유지한다.
- 외부 의존성을 추가하지 않는다.
- 리그 JSON `version: 2` 호환을 유지한다.
- 브라우저 전용 API와 순수 로직을 분리해 테스트 가능한 부분부터 검증한다.

## 선택지 비교

1. ES modules로 분리
   - 장점: 의존성이 명시적이고 테스트하기 쉽다.
   - 단점: `file://` 직접 실행에서 CORS/모듈 로딩 문제가 생길 수 있다.
   - 위험: 사용자가 서버 없이 열 때 앱이 깨질 수 있다.

2. 전역 네임스페이스 기반 다중 `<script>` 분리
   - 장점: 빌드 없이 정적 실행을 유지하면서 파일 크기 제한을 만족한다.
   - 단점: 로딩 순서 의존성이 남는다.
   - 위험: 네임스페이스 누락 시 런타임 오류가 나므로 `index.html` 순서를 문서화해야 한다.

선택: 2번. 현재 MVP 제약에서는 실행 안정성이 더 중요하고, 이후 로컬 개발 서버를 도입할 때 ES modules로 전환한다.

## 변경 영향 메모

- `app.js` 전역 심볼은 `scripts/*.js`의 `window.Animotion` 네임스페이스로 이동한다.
- `index.html`은 단일 `app.js` 대신 순서가 있는 여러 스크립트를 로드한다.
- `styles.css`는 `styles/base.css`, `styles/layout.css`, `styles/components.css`로 분리한다.
- `geometry` 순수 함수는 브라우저와 Node 테스트에서 같이 쓰도록 CommonJS export를 제공한다.
- DOM id, 저장 JSON 구조, 사용자 조작 흐름은 유지한다.

## Arm Attachment Change Note

- Context: 팔 파츠는 몸통을 부모로 잡아도 기본 피벗이 파츠 중앙 상단이라 어깨가 아니라 팔 중간이 붙는 느낌이 날 수 있다.
- Options: 모션 렌더링에서 팔을 프레임마다 보정하거나, 파츠 생성 시 몸통 방향을 보고 기본 피벗을 어깨 쪽 안쪽 모서리로 잡는다.
- Decision: 기본 리깅 데이터가 명시적으로 좋아지는 두 번째 방식을 선택한다. 기존 저장 리그의 피벗은 사용자가 만든 데이터이므로 자동 변경하지 않는다.

## Leg Rigging Change Note

- Context: 하체 파츠도 몸통에 붙어 움직여야 하므로 `leg` 타입, 부모 추정, 골반 쪽 기본 피벗, 가벼운 모션 템플릿 반응이 필요하다.
- Options: 팔/다리를 하나의 limb 추상화로 통합하거나, 지금 구조에 맞춰 `leg` 타입을 명시적으로 추가한다.
- Decision: 현재 MVP는 타입별 동작이 명시적인 구조이므로 `leg` 타입을 추가한다. 과한 추상화는 브러시/메시 단계 이후로 미룬다.

## Custom Motion Authoring Note

- Context: 사용자가 리깅된 파츠별로 직접 움직임을 만들 수 있어야 한다.
- Options: 타임라인 키프레임 에디터를 만들거나, 선택 파츠별 루프 모션 파라미터를 저장한다.
- Decision: MVP에는 파츠별 X/Y 이동, 회전, 세로 스케일, 위상 값을 저장하는 루프 모션을 추가한다. 키프레임 타임라인은 별도 기능으로 미룬다.

## Joint Pivot Change Note

- Context: 몸에 붙은 피벗은 회전축으로 유지하고, 팔꿈치/무릎 같은 관절 피벗은 직접 움직여 파츠 방향을 제어해야 한다.
- Options: 메시/IK 변형을 구현하거나, rigid 파츠에서 관절 목표점 이동을 회전 보정으로 변환한다.
- Decision: 현재 MVP 구조에 맞춰 `pivot`은 부착 회전축, `joint`는 움직이는 관절 기준점으로 분리한다. 직접 만든 모션의 관절 이동값은 `pivot -> joint` 방향 변화로 회전값에 합산한다.

## Blender-Like Keyframe Change Note

- Context: 블렌더식 모션 제작의 핵심은 프레임, 키프레임 삽입/삭제, 채널 보간이다.
- Options: 도프시트/그래프 에디터 전체를 만들거나, 현재 파츠 속성 패널을 프레임 기반 키프레임 포즈 편집기로 확장한다.
- Decision: MVP에는 프레임 슬라이더와 파츠별 키프레임 배열을 추가한다. 각 키프레임은 X/Y, 회전, 스케일, 관절 이동 채널을 저장하고 선형 보간한다.

## Joint Drag Change Note

- Context: 미리보기에서 관절 피벗을 한 번 클릭하는 방식만으로는 실제 리깅 프로그램처럼 위치를 정교하게 맞추기 어렵다.
- Options: IK/포즈 타깃 드래그를 구현하거나, 선택 파츠의 몸 고정 피벗/관절 피벗 자체를 포인터 드래그로 직접 이동한다.
- Decision: 이번 단계에서는 피벗 좌표 자체를 드래그로 편집한다. 클릭한 파란 관절 핸들을 우선 잡고, 빈 곳 클릭은 기존처럼 선택된 편집 대상 피벗을 이동한다.

## Pose Assist Change Note

- Context: 목표는 정지 웹툰 컷을 임팩트 포즈로 보고, 준비동작과 복귀를 2D 애니메이션용 키프레임으로 자동 생성하는 것이다.
- Options: 물리/IK 기반 포즈 예측을 바로 만들거나, 선택 파츠를 액션 기준으로 삼아 스파인/몸통/사지에 타입별 반대동작 키프레임을 생성한다.
- Decision: MVP에는 두 번째 방식을 적용한다. 원본 컷은 0값 임팩트 포즈로 유지하고, 이전 프레임에 anticipation, 이후 프레임에 settle 키를 자동 삽입한다.

## Control Rig Change Note

- Context: 2D 애니메이션에 특화된 블렌더가 되려면 팔/다리 관절을 당겼을 때 스파인, 몸통, 머리, 보조 사지가 함께 반응해야 한다.
- Options: 본격 IK와 제약 솔버를 구현하거나, 키프레임 모드의 관절 드래그를 컨트롤 입력으로 삼아 타입별 연쇄 포즈를 계산한다.
- Decision: 현재는 두 번째 방식을 선택한다. 키프레임 모드에서 관절 핸들을 드래그하면 선택 사지의 `jointX/Y`와 관련 파츠 포즈를 갱신하고, 드래그 종료 시 현재 프레임 키로 저장한다.

## 목표

이 MVP는 빌드 과정 없이 `index.html`을 바로 열어 실행할 수 있어야 한다. 리팩토링의 목표는 현재 동작을 유지하면서 브러시 마스크, AI 보조 세그멘테이션, 메시 변형, 모션 템플릿 확장으로 넘어갈 수 있는 코드 구조를 만드는 것이다.

## 제약

- 당장은 정적 실행 방식을 유지한다. 로컬 `file://` 환경에서 ES module import가 브라우저마다 다르게 막힐 수 있으므로 아직 여러 JS 모듈로 쪼개지 않는다.
- 기능 표면이 안정되기 전까지 외부 의존성을 추가하지 않는다.
- 현재 리그 JSON 스키마 `version: 2`를 유지하고, 기존 사각형 기반 파츠도 계속 불러올 수 있게 한다.
- 큰 웹툰 컷에서도 캔버스 렌더링과 포인터 편집이 충분히 가볍게 돌아야 한다.

## 현재 문제

- `app.js`에 DOM 조회, 앱 상태, 도형 계산, 영역선 편집, 파츠 생성, 렌더링, 내보내기, 이벤트 바인딩이 한꺼번에 들어 있다.
- 줌 배율, 히트 판정 거리, 내보내기 길이, 최소 선택 영역 크기 같은 값이 코드 곳곳에 숫자로 흩어져 있었다.
- 이미지 업로드 초기화, 리그 직렬화, 파츠 캔버스 재생성, 선택 파츠 조회가 UI 갱신 흐름과 강하게 얽혀 있었다.
- 포인터 핸들러 안에 여러 편집 모드가 직접 섞여 있어 브러시나 변형 도구를 추가하기 어렵다.

## 1차 리팩토링 계획

1. 상수와 DOM 선택자를 중앙화한다.
   - 선택 도구 이름, 드래그 모드, 도형 종류, 줌 설정, 내보내기 설정, 최소 크기 기준을 이름 있는 상수로 둔다.
   - 사용자에게 보이는 한국어 문구는 HTML 쪽에 남기고, 동작 코드는 상태값만 다룬다.

2. 데이터 경계를 분명히 한다.
   - shape는 원본 이미지 좌표계 데이터로 취급한다.
   - mask는 파츠 내부 좌표계 데이터로 취급한다.
   - view는 캔버스 표시 좌표 변환으로 취급한다.
   - `state.parts`를 유일한 파츠 컬렉션으로 유지한다.

3. 작은 도메인 파일로 분리한다.
   - 새 이미지 로드 초기화, 선택 파츠 갱신, 파츠 캔버스 재생성, 리그 payload 생성, 리그 파츠 복원 로직을 함수로 분리한다.
   - `scripts/*.js`는 `window.Animotion` 네임스페이스를 공유하고 `index.html`의 로딩 순서로 의존성을 관리한다.

4. 이벤트 핸들러를 얇게 만든다.
   - 캔버스 이벤트, 미리보기 이벤트, 컨트롤 이벤트, 윈도우 이벤트 바인딩을 함수로 묶는다.
   - 포인터 핸들러는 모드를 판정하고 명명된 편집 함수로 위임한다.

5. 매 단계 검증한다.
   - `node --check`를 모든 `scripts/*.js`, `tests/*.js`에 실행한다.
   - `node tests/geometry.test.js`로 순수 geometry 테스트를 실행한다.
   - 수동 확인 경로는 이미지 업로드, 각 선택 도구 사용, 점 편집, 파츠 생성, 피벗 설정, 리그 저장/불러오기, WebM 내보내기다.

## 다음 리팩토링 후보

- 로컬 개발 서버를 도입하면 ES modules로 실제 파일 분리를 진행한다.
- 브러시 마스크를 구현하기 전에 캔버스 상호작용을 명시적인 state machine으로 분리한다.
- source stage renderer와 preview stage renderer를 별도로 분리한다.
- 테스트 러너를 도입하면 순수 geometry 함수부터 단위 테스트를 추가한다.
