# 두 컷 컷신 브리지 기획

## Problem 1-Pager

### Context

Animotion은 현재 정적 브라우저 MVP다. 웹툰 컷에서 파츠를 떼어내고, 피벗과 관절을 지정하고, 간단한 모션을 만든 뒤 WebM으로 내보낼 수 있다. `lookism` 프로토타입은 영상 파일을 재생하지 않고도 원본 컷아웃, 리깅 파츠, 키포즈, 속도선, 잔상, 플래시, 흔들림, 최종 임팩트 컷을 합성해서 짧고 강한 컷신을 만들 수 있음을 보여준다.

### Problem

다음 목표는 정지 이미지 하나를 흔드는 것이 아니다. 유저가 웹툰 두 장면을 입력했을 때, 사람이 연속 컷을 연결해서 상상하듯이 앱도 A 컷을 시작, B 컷을 임팩트/도착으로 보고 그 사이를 짧은 컷신으로 이어야 한다. 다만 첫 단계에서 완전 자동화는 현실적이지 않다. 웹툰 그림에서는 파츠 분리, A/B 파츠 대응, 피벗 배치, 동작 의도가 자주 애매하기 때문이다.

### Goal

앱이 A 컷 파츠, 피벗, 모션 비트, 컷신 효과의 초안을 만들고, 유저가 중요한 판단을 전부 리터칭할 수 있는 반자동 컷신 저작 흐름을 만든다. 첫 품질 목표는 `lookism`처럼 A 컷 파츠로 짧은 중간 브리지를 만들고, B 컷 전체를 최종 임팩트로 선명하게 스냅하는 것이다.

### Non-Goals

- 1차 목표에서 원클릭 완전 자동 영상 생성기를 만들지 않는다.
- 브라우저 안에서 커스텀 AI 모델을 학습하지 않는다.
- 핵심 MVP에 빌드 파이프라인이나 서버를 강제하지 않는다.
- 기존 수동 파츠 편집 흐름을 제거하지 않는다.
- 생성형 비디오를 주 엔진으로 삼지 않는다.

### Constraints

- AI 백엔드가 필요해지는 별도 단계 전까지는 로컬 `file://` 실행 흐름을 유지한다.
- 기존 리그 JSON 호환성을 깨지 않는다.
- 변경은 작고 테스트 가능하게 나눈다.
- 코드 파일은 가능한 한 기존 300 LOC 기준을 지킨다.
- AI 또는 알고리즘 결과는 확정값이 아니라 유저 승인/수정 가능한 제안으로 취급한다.

## 제품 방향

Animotion은 반자동 웹툰 컷신 에디터가 되어야 한다.

```text
A 컷 업로드
-> B 컷 업로드
-> A 컷에서 파츠를 만들거나 추천받음
-> B 컷을 임팩트 패널로 정렬
-> A 파츠로 짧은 브리지 컷신 생성
-> 마지막에 B 컷 전체로 스냅
-> 피벗, 관절, 타이밍, 효과 리터칭
-> WebM 내보내기
```

핵심 약속은 "두 웹툰 순간을 `lookism`식 편집 가능한 브리지 컷신으로 연결한다"이다.

## 품질 원칙

`lookism` 퀄리티는 좁고 통제된 트릭에서 나온다.

```text
A 컷을 선명하게 보여준다
-> 완벽하지 않은 중간 모션은 속도, 잔상, 효과로 감춘다
-> B 컷으로 선명하게 스냅한다
```

앱도 이 전략을 따라야 한다. 중간 프레임은 오래 감상할 완성 작화일 필요가 없다. 방향, 무게, 임팩트를 설득한 뒤 최종 B 컷이 화면을 가져가면 된다.

## 선택지 비교

### 선택지 1: A 컷 파츠만 B 컷 방향으로 움직인다

- 장점: 데이터 모델이 단순하고 현재 `parts`, `keyframes`를 바로 재사용할 수 있다.
- 단점: 큰 포즈 변화에서는 최종 형태가 B 컷 작화와 잘 맞지 않는다.
- 위험: A 컷 컷아웃을 억지로 B 포즈처럼 만들면서 품질이 떨어질 수 있다.

### 선택지 2: A 컷과 B 컷에서 모두 파츠를 딴다

- 장점: 임팩트 순간의 작화 일관성이 살아나고, B 파츠 플래시와 최종 스냅을 만들 수 있다.
- 단점: 파츠 대응과 명시적인 트랙 모델이 필요하다.
- 위험: A/B 파츠 연결이 틀릴 수 있으므로 리터칭 UI가 필요하다.

결정: 1차 구현은 선택지 1을 택한다. `lookism`처럼 A 파츠로 중간을 만들고 B 전체 컷으로 임팩트를 책임지게 하면 구현 범위가 작고 품질 전략도 명확하다. 선택지 2는 고품질 확장 단계로 둔다.

### 선택지 3: 생성형 보간을 주 엔진으로 쓴다

- 장점: 복잡한 중간 프레임을 새로 만들 수 있다.
- 단점: 제어가 약하고 선화나 캐릭터 정체성이 바뀔 수 있다.
- 위험: 결과가 그럴듯해 보여도 유저의 원본 컷에 충실하지 않을 수 있다.

결정: 생성형 보간은 미래 보조 기능으로 남기고, 주 엔진으로 삼지 않는다.

## 핵심 데이터 모델

1차 구현의 중요한 단위는 `cutsceneBridge`다. B 파츠 대응 없이도 A 파츠, 주역 파츠, 임팩트 패널, 효과 타이밍을 하나의 컷신 설정으로 묶는다.

```js
{
  id: "bridge_01",
  primaryPartId: "right_leg",
  impactImageRole: "full-panel-snap",
  durationFrames: 18,
  beats: ["hold", "anticipation", "launch", "bridge", "impact"],
  effectDirection: { x: 1, y: -0.25 },
  effectStrength: 1,
  impactFrame: 15
}
```

이 설정이 있으면 앱은 `lookism`식으로 컷신을 만들 수 있다.

- 어떤 A 파츠가 액션을 주도하는지
- 준비동작과 발사 타이밍이 어디인지
- 속도선과 잔상이 어느 방향으로 가야 하는지
- B 전체 컷이 언제 화면을 가져가는지

고품질 확장 단계에서는 `partTrack`을 추가해 A 파츠와 B 파츠를 직접 연결한다.

## 제안 엔진

### Part Proposal Engine

역할:

- 중요한 신체 파츠의 마스크 또는 박스 후보를 만든다.
- 위치, 크기, 부모 관계를 보고 파츠 타입을 추정한다.
- 부모, 피벗, 관절 위치를 제안한다.
- 확신도와 함께 보여주고 유저가 리터칭하게 한다.

1차 구현은 휴리스틱과 수동 편집을 유지한다. 이후 SAM 계열 세그멘테이션을 붙이면 마스크 초안을 더 잘 만들 수 있다.

### Track Matching Engine

역할:

- A 파츠와 B 파츠를 짝짓는다.
- 추천 연결을 캔버스 위에 시각화한다.
- 유저가 승인, 거부, 다시 연결할 수 있게 한다.
- 확신도와 승인 상태를 저장한다.

1차 구현에는 포함하지 않는다. B 파츠를 쓰는 고품질 확장 단계에서 수동 연결에 타입/상대 위치 기반 추천을 더하는 정도로 시작한다.

### Motion Bridge Engine

역할:

- A 파츠와 주역 파츠를 키프레임으로 바꾼다.
- 준비, 발사, 브리지, 임팩트, 정착 비트를 만든다.
- 주역 파츠가 먼저 움직이고 보조 파츠가 반응하게 한다.
- 중간 모션을 짧고 읽기 쉽게 유지한다.

1차 구현에서는 A 파츠와 선택된 주역 파츠를 기준으로 키프레임을 만든다. 기존 `timeline`과 `pose-assist`를 대체하지 말고 그 위에 얹는다.

### Cutscene Direction Engine

역할:

- 컷신 시간을 0~1로 정규화한다.
- 주역 트랙 이동 방향으로 속도선을 만든다.
- 이동 파츠 뒤에 잔상을 배치한다.
- 카메라 흔들림, 임팩트 플래시, 패널 테두리, 최종 B 컷 스냅을 제어한다.

타이밍과 효과 계산은 결정적 순수 함수로 두어 테스트 가능하게 만든다.

## 1차 구현 범위

첫 구현은 작게 유지한다.

1. 두 번째 업로드 이미지를 단순 고스트가 아니라 임팩트 패널로 승격한다.
2. `cutsceneBridge` 설정을 추가해 주역 파츠, duration, impact frame, 효과 방향을 저장한다.
3. `웹툰 컷신` 모션 모드를 추가한다.
4. 선택된 주역 A 파츠 기준으로 짧은 브리지 컷신을 생성한다.
5. 속도선, 잔상, 플래시, 흔들림, 최종 B 컷 스냅을 합성한다.
6. 기존 미리보기 Canvas 기반 WebM 내보내기를 유지한다.

## 리터칭 흐름

앱은 추론 결과를 숨기면 안 된다. 다음 오버레이를 편집 가능하게 보여줘야 한다.

- A 파츠 마스크
- 피벗과 관절 핸들
- 주역 이동 방향 화살표
- 생성된 비트 마커
- 효과 방향과 강도 핸들
- B 컷 스냅 타이밍과 블렌드 구간

유저는 틀린 판단 하나만 고쳐도 전체 흐름을 다시 만들 수 있어야 한다.

## AI 보조 로드맵

### Phase 1: 외부 AI 없음

- 수동 마스크
- 휴리스틱 피벗/관절
- A 파츠 기반 `lookism`식 브리지
- 결정적 컷신 생성

### Phase 2: 로컬 또는 백엔드 세그멘테이션

- SAM 계열 마스크 후보
- 클릭 또는 박스 프롬프트 기반 분리
- 유저 승인 마스크 보정

### Phase 3: 대응점과 추적

- 특징점 또는 포인트 매칭으로 A/B 파츠 연결 추천
- 피벗과 관절의 두 컷 간 이전
- 확신도 시각화
- A/B `partTrack` 도입

### Phase 4: 프로젝트 메모리

- 유저가 고친 마스크, 피벗, 관절, 파츠 이름을 캐릭터별로 저장한다.
- 이후 컷에서 승인된 구조를 재사용한다.
- 모델 학습 없이도 가벼운 학습처럼 동작하게 한다.

### Phase 5: 선택적 생성형 보조

- 거친 인비트윈 또는 스미어 프레임을 제안한다.
- 생성 결과를 유일한 원본으로 삼지 않는다.
- 편집 가능한 파츠 트랙 흐름을 유지한다.

## 구현 메모

- 현재 `state.parts`는 기존 동작을 위해 유지한다.
- `cutsceneBridge` 데이터는 기존 리그 JSON을 깨지 않게 선택 필드로 추가한다.
- `partTrack` 데이터는 B 파츠를 쓰는 확장 단계에서 추가한다.
- 현재 `timeline`은 키프레임 평가기로 계속 사용한다.
- 컷신 효과는 별도 Canvas 헬퍼로 분리한다.
- 타이밍과 효과 계산은 테스트 가능한 결정적 수학 함수로 둔다.
- 최종 B 컷은 단순 고스트가 아니라 풀프레임 임팩트 레이어로 취급한다.

## 열린 질문

- 최종 B 컷은 임팩트에서 즉시 교체할지, 몇 프레임 블렌드 후 교체할지 정해야 한다.
- 내보내기 길이는 전역 설정을 쓸지, 컷신별 duration을 둘지 정해야 한다.
- 첫 컷신 프리셋은 공격 액션 중심으로 둘지, 일반 이동/시선/대화 컷도 포함할지 정해야 한다.
- 고품질 확장에서 B 파츠 편집 화면을 기존 원본 캔버스에 넣을지, A/B 소스 탭을 따로 둘지 정해야 한다.
